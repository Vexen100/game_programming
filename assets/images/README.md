# Images

External production assets are not included yet.

`ResourceManager` can render generated placeholder surfaces for tiles and
entities, so the game does not depend on image files at this stage.

Real sprites can be added here later. Do not store user saves, settings files,
or other runtime data in this directory.
